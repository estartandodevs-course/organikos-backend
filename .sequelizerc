const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'src', 'api', 'database', 'database.js'),
    'migrations-path': path.resolve(
        __dirname,
        'src',
        'api',
        'database',
        'migrations'
    ),
};
